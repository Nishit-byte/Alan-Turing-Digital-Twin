# ── Web framework & server ─────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
sse-starlette==2.1.3          # for streaming chat responses (SSE)

# ── Environment / config ───────────────────────────────
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2

# ── LLM (Gemini) ────────────────────────────────────────
google-generativeai==0.8.3

# ── RAG / retrieval pipeline ────────────────────────────
langchain==0.3.7
langchain-community==0.3.5
langchain-text-splitters==0.3.2
sentence-transformers==3.2.1   # embeddings + cross-encoder reranker
chromadb==0.5.20

# ── PDF / document processing ───────────────────────────
pypdf==5.0.1
pdfplumber==0.11.4             # fallback for tricky PDF layouts
pytesseract==0.3.13            # OCR for scanned Turing Archive PDFs
pdf2image==1.17.0              # convert scanned PDF pages to images for OCR
beautifulsoup4==4.12.3         # cleaning Wikipedia/HTML source text
lxml==5.3.0

# ── Memory / database ───────────────────────────────────
sqlalchemy==2.0.35
aiosqlite==0.20.0

# ── Utilities ────────────────────────────────────────────
tiktoken==0.8.0                # token counting for chunking
tenacity==9.0.0                # retry logic for Gemini API calls
loguru==0.7.2                  # cleaner logging than stdlib

# ── Dev / testing ────────────────────────────────────────
pytest==8.3.3
httpx==0.27.2                  # for testing FastAPI endpoints